# <Tutorial title>

- **Instructor:** <name>
- **When:** Day <1–5>, <weekday July 27–31>, <time> — "<session name in schedule>"
- **Stack:** <e.g. Python 3.11 · marimo + uv | Jupyter | Colab | MATLAB R2024b | none — slides only>

## Run

<!-- EXACTLY ONE command. Or delete this section and write:
     "Materials only, not runnable." -->
```bash
<one command, e.g.: uvx marimo edit --sandbox notebook.py>
```

## Requirements

<!-- What must exist before the run command works: uv? MATLAB + toolbox?
     nothing? Data downloaded at runtime (from where, pinned how)?
     Rough runtime/RAM. Keep it to a paragraph. -->

## Files

- `<file>` — <what it is; say which file is the source of truth>
